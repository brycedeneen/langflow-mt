# Starter project JSON files have been removed; they now live in the DB as Template rows.
# The load_starter_projects function has been removed along with the JSON source files.
# Tests that depended on load_starter_projects have been removed.

