# Sentry error-reporting (optional).
# When VITE_SENTRY_DSN is absent the Sentry adapter is a no-op and
# the build is fully clean.  Set it in production deployments only.
VITE_SENTRY_DSN=
# Override the Sentry "environment" tag (defaults to Vite's MODE).
VITE_SENTRY_ENV=
